# EcoSat Sustainable FPGA CubeSat

EcoSat is a low-power modular electronics subsystem for 1U-3U CubeSats and
NanoSats in Low Earth Orbit. It addresses **AESS Sustainability Hackathon 2026,
Challenge 1: Sustainable Electronics for Space Systems** by replacing a
monolithic always-on electronics stack with orbit-aware power control,
plug-and-play modules, and optional FPGA burst acceleration.

The core claim is simple: the satellite electronics should not consume full
power when the mission state does not require it. EcoSat reduces average energy
use by switching modules between active, low-rate, sleep, and off modes across
sunlight and eclipse phases.

## Key Results

These values are generated by `python3 simulation/main.py`.

| Scenario | Average power | Energy per orbit | Energy saved vs baseline |
| --- | ---: | ---: | ---: |
| Baseline always-on electronics | `242.55 mW` | `384.04 mWh` | `0.00%` |
| Modular duty-cycled design | `126.54 mW` | `200.35 mWh` | `47.83%` |
| MCU + FPGA burst design | `70.41 mW` | `111.49 mWh` | `70.97%` |

The **modular duty-cycled design** is the conservative main result. The
**MCU + FPGA burst design** is an extension scenario and depends on the
assumption that FPGA compression/filtering reduces radio active time.

## System Overview

EcoSat is organized as five cooperative modules:

| Module | Role | Main components |
| --- | --- | --- |
| OBC module | Scheduling, PMEP management, orbit-state policy | STM32L496, FRAM, RTC |
| Power module | Rail switching, battery monitoring, solar regulation | INA3221, TPS63020, LTC4412 |
| Sensing module | Environmental and mission sensing | TMP117, ADXL345, photodiodes |
| Comms module | UHF telemetry uplink/downlink | CC1120, PA/LNA |
| FPGA accelerator | Burst processing for high-value workloads | Spartan FPGA |

The FPGA is not treated as an always-on controller. The STM32 owns mission
scheduling and power policy; the FPGA rail is enabled only for short workloads
such as FIR filtering, run-length telemetry compression, CAN packet filtering,
and fault-detection logic.

## Power Strategy

EcoSat combines:

- Orbit-aware duty cycling between sunlight and eclipse
- Per-module switched power rails
- Low-rate sensing during eclipse
- Comms shutdown when downlink is not needed
- Adaptive MCU operating mode
- Optional FPGA burst activation for compute-heavy tasks

The baseline assumes a conventional monolithic board where the MCU, sensors,
radio, memory, bus logic, and power-management blocks remain active for the full
95-minute orbit.

## Quickstart

From the repository root:

```bash
python3 simulation/main.py
python3 tests/run_tests.py
```

The first command regenerates the simulation outputs. The second command runs a
zero-dependency test suite for the core model, orbit logic, scheduler policy,
FPGA activation policy, and energy calculations.

If you install the optional Python dependencies:

```bash
python3 -m pip install -r requirements.txt
python3 -m pytest
```

## Generated Outputs

Simulation outputs are written under `results/`:

| File | Purpose |
| --- | --- |
| `results/csv/baseline_power.csv` | Baseline always-on power and energy |
| `results/csv/optimized_power.csv` | Scenario-level optimized power metrics |
| `results/csv/orbit_energy.csv` | Energy-per-orbit comparison |
| `results/csv/battery_lifetime.csv` | Battery-cycle and extension estimates |
| `results/csv/scenario_phase_power.csv` | Phase-by-phase load breakdown |
| `results/reports/simulation_summary.md` | Human-readable result summary |

## RTL Evidence

The FPGA implementation scaffold is in `rtl/`:

| Folder | Contents |
| --- | --- |
| `rtl/fir_filter/` | FIR filter datapath and testbench |
| `rtl/compression/` | RLE compressor, packet encoder, telemetry buffer, testbench |
| `rtl/can_filter/` | CAN packet classifier/filter and testbench |
| `rtl/top/` | Orbit controller, scheduler, power controller, top-level wrapper |
| `rtl/common/` | FIFO, clock divider, edge detector, debounce, UART debug |
| `rtl/simulations/` | Icarus Verilog, ModelSim, and Vivado notes/scripts |
| `rtl/synthesis_reports/` | Placeholder synthesis, timing, and power reports |

If Icarus Verilog is installed:

```bash
./rtl/simulations/iverilog/run_iverilog.sh
```

This environment did not include `iverilog` during setup, so RTL execution should
be rerun on a machine with Icarus Verilog, ModelSim, or Vivado installed.

## Repository Map

```text
EcoSat-Sustainable-FPGA-CubeSat/
├── docs/                 Technical design, equations, diagrams, references
├── rtl/                  Verilog FPGA accelerator modules and testbenches
├── simulation/           Python orbit, power, scheduler, subsystem models
├── results/              Generated CSVs, reports, graph placeholders
├── presentation/         Pitch notes, demo script, slide visuals
├── video/                Narration script and storyboard
├── tests/                Zero-dependency Python tests
├── assets/               Logos, screenshots, icons, diagram assets
├── requirements.txt
├── LICENSE
└── README.md
```

## Challenge Alignment

| Challenge requirement | EcoSat evidence |
| --- | --- |
| One well-defined subsystem | Modular CubeSat electronics subsystem |
| Baseline vs optimized comparison | `simulation/main.py`, `simulation/power/`, `results/csv/` |
| Power-saving strategy | Duty cycling, rail switching, sleep modes, FPGA burst activation |
| Implementation evidence | Python model, tests, Verilog RTL modules, RTL testbenches |
| Sustainability argument | Energy savings, battery-cycle reduction, thermal-load reduction, modular reuse |
| Reproducibility | `python3 simulation/main.py` and `python3 tests/run_tests.py` |
